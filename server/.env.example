PORT=5000
NODE_ENV=development
# Comma-separated list of allowed origins for production (not used in dev mode)
# Example: CLIENT_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CLIENT_ORIGINS=
